/** @type {import('next').NextConfig} */
const nextConfig = {
  turbopack: {}, // 👈 Esto elimina el error
  // ... cualquier otra configuración que tengas (webpack, etc.)
};

export default nextConfig;
